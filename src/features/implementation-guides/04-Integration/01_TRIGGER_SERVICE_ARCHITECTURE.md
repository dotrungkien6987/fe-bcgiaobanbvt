# 🔔 Notification Trigger Service - Kiến trúc hệ thống

> **Mục tiêu**: Xây dựng hệ thống Centralized Config File - một file duy nhất chứa toàn bộ cấu hình notification triggers. Cho phép bật/tắt thông báo bằng config, giữ code tích hợp tối giản (1 dòng mỗi trigger).

---

## 📊 Tổng quan kiến trúc

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           NOTIFICATION TRIGGER SYSTEM                            │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│   ┌──────────────┐     ┌──────────────────┐     ┌─────────────────────────────┐ │
│   │   Business   │     │  TriggerService  │     │   NotificationService       │ │
│   │    Logic     │────▶│                  │────▶│   (Existing)                │ │
│   │              │     │  fire(key, ctx)  │     │   .send()                   │ │
│   └──────────────┘     └────────┬─────────┘     │   .sendToMany()             │ │
│                                 │               └─────────────────────────────┘ │
│                                 │                              │                │
│                                 ▼                              ▼                │
│                    ┌────────────────────────┐     ┌─────────────────────────┐   │
│                    │  notificationTriggers  │     │    Socket.IO / FCM      │   │
│                    │       .js (Config)     │     │    (Delivery Layer)     │   │
│                    └────────────────────────┘     └─────────────────────────┘   │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🗂️ Cấu trúc file Backend

```
giaobanbv-be/
├── config/
│   └── notificationTriggers.js    ← 🆕 Centralized config (11 triggers)
│
├── services/
│   └── triggerService.js          ← 🆕 Core service với fire() method
│
├── helpers/
│   └── notificationHelper.js      ← 🆕 NhanVienID → User._id mapping
│
├── modules/workmanagement/
│   ├── services/
│   │   ├── congViec.service.js    ← 📝 Thêm 3 trigger calls
│   │   └── notificationService.js ← ✅ Existing (không đổi)
│   │
│   └── controllers/
│       └── kpi.controller.js      ← 📝 Thêm 4 trigger calls
│
└── seeds/
    └── notificationTemplates.js   ← 📝 Thêm KPI_APPROVAL_REVOKED
```

---

## 🔄 Flow xử lý Trigger

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              TRIGGER FLOW DIAGRAM                                │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│   ① Business Logic gọi trigger                                                  │
│   ─────────────────────────────                                                 │
│                                                                                  │
│   congViec.service.js                    kpi.controller.js                      │
│   ┌─────────────────────┐                ┌─────────────────────┐                │
│   │ await triggerService│                │ await triggerService│                │
│   │   .fire(            │                │   .fire(            │                │
│   │     "CongViec.xxx", │                │     "KPI.xxx",      │                │
│   │     { congViec,     │                │     { danhGiaKPI,   │                │
│   │       performerId } │                │       performerId } │                │
│   │   );                │                │   );                │                │
│   └─────────┬───────────┘                └─────────┬───────────┘                │
│             │                                      │                            │
│             └──────────────────┬───────────────────┘                            │
│                                ▼                                                │
│   ② TriggerService xử lý                                                        │
│   ──────────────────────                                                        │
│   ┌─────────────────────────────────────────────────────────────────────────┐  │
│   │                                                                          │  │
│   │  Step 1: Check enabled                                                   │  │
│   │  ┌────────────────────────────────────────────────────────────────────┐ │  │
│   │  │ const config = triggers[triggerKey];                               │ │  │
│   │  │ if (!config?.enabled) return; // ← Skip nếu disabled               │ │  │
│   │  └────────────────────────────────────────────────────────────────────┘ │  │
│   │                                      │                                   │  │
│   │                                      ▼                                   │  │
│   │  Step 2: Get handler & resolve recipients                               │  │
│   │  ┌────────────────────────────────────────────────────────────────────┐ │  │
│   │  │ const handler = handlers[config.handler];                          │ │  │
│   │  │ const { recipientNhanVienIds, data } = handler(context);           │ │  │
│   │  └────────────────────────────────────────────────────────────────────┘ │  │
│   │                                      │                                   │  │
│   │                                      ▼                                   │  │
│   │  Step 3: Convert NhanVienID → User._id                                  │  │
│   │  ┌────────────────────────────────────────────────────────────────────┐ │  │
│   │  │ const userIds = await notificationHelper                           │ │  │
│   │  │   .resolveNhanVienListToUserIds(recipientNhanVienIds);             │ │  │
│   │  └────────────────────────────────────────────────────────────────────┘ │  │
│   │                                      │                                   │  │
│   │                                      ▼                                   │  │
│   │  Step 4: Exclude performer (nếu config.excludePerformer = true)         │  │
│   │  ┌────────────────────────────────────────────────────────────────────┐ │  │
│   │  │ if (config.excludePerformer && performerUserId) {                  │ │  │
│   │  │   userIds = userIds.filter(id => id !== performerUserId);          │ │  │
│   │  │ }                                                                  │ │  │
│   │  └────────────────────────────────────────────────────────────────────┘ │  │
│   │                                      │                                   │  │
│   │                                      ▼                                   │  │
│   │  Step 5: Send notifications                                             │  │
│   │  ┌────────────────────────────────────────────────────────────────────┐ │  │
│   │  │ await notificationService.sendToMany({                             │ │  │
│   │  │   type: config.template,                                           │ │  │
│   │  │   recipientIds: userIds,    // ← User._id (QUAN TRỌNG!)            │ │  │
│   │  │   data: data                                                       │ │  │
│   │  │ });                                                                │ │  │
│   │  └────────────────────────────────────────────────────────────────────┘ │  │
│   │                                                                          │  │
│   └─────────────────────────────────────────────────────────────────────────┘  │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## ⚠️ Quan trọng: NhanVienID vs User.\_id

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                    CHUYỂN ĐỔI NhanVienID → User._id                              │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│   CongViec / DanhGiaKPI                    User Model                            │
│   ┌────────────────────┐                   ┌────────────────────┐               │
│   │ NguoiGiaoViecID    │───┐               │ _id ◄──────────────│── recipientId │
│   │ NguoiChinhID       │   │   lookup      │ NhanVienID ────────│── (foreign)   │
│   │ NguoiThamGia[]     │   └──────────────▶│ HoTen              │               │
│   │ NhanVienID (KPI)   │                   │ Email              │               │
│   └────────────────────┘                   └────────────────────┘               │
│          ↓                                          ↑                            │
│     NhanVien._id                                    │                            │
│                                                     │                            │
│   ⚠️ Business models dùng NhanVienID                │                            │
│   ⚠️ Notification system cần User._id ─────────────┘                            │
│                                                                                  │
│   📄 helpers/notificationHelper.js                                              │
│   ────────────────────────────────                                              │
│   • resolveNhanVienToUserId(nhanVienId)                                         │
│   • resolveNhanVienListToUserIds(nhanVienIds[])                                 │
│   • getDisplayName(nhanVienId)                                                  │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 📋 Danh sách 15 Triggers

### CongViec Triggers (11 triggers, 10 đang hoạt động)

| #   | Trigger Key                   | Template                | Vị trí code             | Trạng thái  |
| --- | ----------------------------- | ----------------------- | ----------------------- | ----------- |
| 1   | `CongViec.giaoViec`           | TASK_ASSIGNED           | giaoViec() ~line 1560   | ✅ Enabled  |
| 2   | `CongViec.GIAO_VIEC`          | TASK_ASSIGNED           | transition()            | ✅ Enabled  |
| 3   | `CongViec.HUY_GIAO`           | TASK_CANCELLED          | transition()            | ✅ Enabled  |
| 4   | `CongViec.TIEP_NHAN`          | TASK_ACCEPTED           | transition()            | ✅ Enabled  |
| 5   | `CongViec.HOAN_THANH`         | TASK_COMPLETED          | transition()            | ✅ Enabled  |
| 6   | `CongViec.HOAN_THANH_TAM`     | TASK_PENDING_APPROVAL   | transition()            | ✅ Enabled  |
| 7   | `CongViec.HUY_HOAN_THANH_TAM` | TASK_REVISION_REQUESTED | transition()            | ✅ Enabled  |
| 8   | `CongViec.DUYET_HOAN_THANH`   | TASK_APPROVED           | transition()            | ✅ Enabled  |
| 9   | `CongViec.TU_CHOI`            | TASK_REJECTED           | transition()            | ⚠️ Disabled |
| 10  | `CongViec.MO_LAI_HOAN_THANH`  | TASK_REOPENED           | transition()            | ✅ Enabled  |
| 11  | `CongViec.comment`            | COMMENT_ADDED           | addComment() ~line 2775 | ✅ Enabled  |

> **Lưu ý:** Trigger `CongViec.TU_CHOI` đang disabled vì action TU_CHOI chưa được implement trong workflow.

### Deadline Triggers (2 - Tự động bởi Agenda.js)

| #   | Trigger Key                     | Template             | Vị trí code   | Trạng thái |
| --- | ------------------------------- | -------------------- | ------------- | ---------- |
| 12  | `CongViec.DEADLINE_APPROACHING` | DEADLINE_APPROACHING | Agenda.js job | ✅ Enabled |
| 13  | `CongViec.DEADLINE_OVERDUE`     | DEADLINE_OVERDUE     | Agenda.js job | ✅ Enabled |

### KPI Triggers (4)

| #   | Trigger Key        | Template             | Vị trí code                  | Mô tả              |
| --- | ------------------ | -------------------- | ---------------------------- | ------------------ |
| 14  | `KPI.taoDanhGia`   | KPI_CYCLE_STARTED    | taoDanhGiaKPI() ~line 90     | Tạo đánh giá KPI   |
| 15  | `KPI.duyetDanhGia` | KPI_EVALUATED        | duyetDanhGiaKPI() ~line 605  | Duyệt KPI          |
| 16  | `KPI.duyetTieuChi` | KPI_EVALUATED        | duyetKPITieuChi() ~line 1745 | Duyệt KPI tiêu chí |
| 17  | `KPI.huyDuyet`     | KPI_APPROVAL_REVOKED | huyDuyetKPI() ~line 2100     | Hủy duyệt KPI      |

---

## 🔌 Extension Points cho tương lai

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         FUTURE EXTENSION POINTS                                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│   ┌─────────────────────────────────────────────────────────────────────────┐   │
│   │                        TriggerService.fire()                             │   │
│   │                               │                                          │   │
│   │           ┌───────────────────┼───────────────────┐                     │   │
│   │           ▼                   ▼                   ▼                     │   │
│   │   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐               │   │
│   │   │ In-App       │   │ FCM Push     │   │ Email        │               │   │
│   │   │ ✅ DONE      │   │ 🔜 PHASE 2   │   │ 📅 PHASE 3   │               │   │
│   │   │              │   │              │   │              │               │   │
│   │   │ Socket.IO    │   │ Firebase     │   │ Nodemailer   │               │   │
│   │   └──────────────┘   └──────────────┘   └──────────────┘               │   │
│   └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│   📝 Thêm module mới:                                                           │
│   ──────────────────                                                            │
│   1. Thêm triggers vào config/notificationTriggers.js                           │
│   2. Thêm handler function trong triggerService                                 │
│   3. Thêm 1-line call trong business logic                                      │
│   4. Done! 🎉                                                                   │
│                                                                                  │
│   Ví dụ: BaoCaoSuCo (Incident Report)                                          │
│   ─────────────────────────────────────                                        │
│   "BaoCaoSuCo.taoMoi": { template: "INCIDENT_CREATED", ... }                   │
│   "BaoCaoSuCo.chapNhan": { template: "INCIDENT_ACCEPTED", ... }                │
│   "BaoCaoSuCo.phanTich": { template: "INCIDENT_ANALYZED", ... }                │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🖥️ Debug & Monitoring

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           DEBUG & MONITORING                                     │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│   📡 API Endpoint: GET /api/notifications/triggers/summary                      │
│   ─────────────────────────────────────────────────────────                     │
│   Response:                                                                      │
│   {                                                                              │
│     "success": true,                                                             │
│     "data": {                                                                    │
│       "total": 15,                                                               │
│       "enabled": 14,                                                             │
│       "disabled": 1,                                                             │
│       "triggers": [                                                              │
│         { "key": "CongViec.giaoViec", "enabled": true, "template": "..." },     │
│         { "key": "CongViec.TU_CHOI", "enabled": false, "template": "..." },     │
│         ...                                                                      │
│       ]                                                                          │
│     }                                                                            │
│   }                                                                              │
│                                                                                  │
│   ─────────────────────────────────────────────────────────────────────────────│
│                                                                                  │
│   🖥️ Console Log khi server start:                                              │
│   ─────────────────────────────────                                             │
│   [TriggerService] ✅ Loaded 15 triggers (14 enabled, 1 disabled)               │
│   [TriggerService] ⚠️  Disabled: CongViec.TU_CHOI                               │
│                                                                                  │
│   🖥️ Console Log khi fire trigger:                                              │
│   ─────────────────────────────────                                             │
│   [TriggerService] 🔔 Firing CongViec.giaoViec → 1 recipients                   │
│   [TriggerService] ✅ CongViec.giaoViec sent successfully                       │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 📚 Tài liệu liên quan

- `02_IMPLEMENTATION_STEPS.md` - Các bước triển khai chi tiết
- `03_CODE_SAMPLES.md` - Code mẫu cho từng file
- `../02-Notification-System/` - Hệ thống notification cơ sở
